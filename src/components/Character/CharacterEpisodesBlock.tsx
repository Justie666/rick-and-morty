import { FC } from 'react'

export const CharacterEpisodesBlock: FC = () => {
  return (
    <div>
      <h2 className='text-2xl font-semibold text-gray-500'>dasdsa</h2>
      {/* <CharacterInformationItem
        key={info.title}
        title={info.title}
        description={info.description}
        isLink={info.isLink}
        date={info.date}
        to={info.to} */}
      {/* /> */}
    </div>
  )
}
